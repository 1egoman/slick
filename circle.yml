dependencies:
	override:
		- go get -u github.com/kardianos/govendor
		- govendor fetch +out
		- go build

test:
  override:
    - go test -v ./... # no -race for now, I guess I have races?

deployment:
  release:
    branch: master
    commands:
		- stat ./slime
